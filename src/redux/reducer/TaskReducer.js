const initialstate = {
  todo: [],
  current: null,
  FilteredTodos: [],
};
const taskReducer = (state = initialstate, { type, payload }) => {
  // switch (type) {
  //     default:
  //         break;
  // }
};
